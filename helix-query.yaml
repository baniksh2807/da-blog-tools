version: 1

indices:
  media:
    include:
      - '/media/**'
    target: /media-index.json
    properties:
      path:
        select: none
        value: path
      name:
        select: none
        value: |
          path.split('/').pop()